# A-Class Experiments

Reserved for A-class circuit topology recognition experiments.

Recommended layout:

```text
cases/<case-id>/
  input/
  reference/
  runs/<method-name>/
```

The A-class skill should be added under `skills/aclass-circuit-recognition/` or another clearly named skill directory.

